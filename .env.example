# Public URL of the deployed FastAPI service. Do not add a trailing slash.
VITE_API_URL="https://your-render-service.onrender.com"

# Server-side values belong on Render, never in Vercel's VITE_ variables.
GEMINI_API_KEY=""
GEMINI_MODEL="gemini-2.5-flash"
