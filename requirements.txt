# Render / FastAPI runtime dependencies.
# The React frontend is built and deployed independently.
fastapi==0.110.0
uvicorn[standard]==0.29.0
pydantic>=2.10.0
python-dotenv==1.0.1
requests==2.31.0
paho-mqtt==2.1.0
